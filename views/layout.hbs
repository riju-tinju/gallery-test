<!DOCTYPE html>
<html>

<head>
  <title>{{title}}</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  
  {{#if admin}}
  {{> admin_header}}
  {{else}}
  {{> user_header}}
  {{/if}}

  {{{body}}}
  
</body>

</html>